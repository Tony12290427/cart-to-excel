# Neural signature tool (tools/neural_signature.py)
# The Graves RNN checkpoint is downloaded on first run and is deliberately not
# stored in this repository — see the licensing note in README.md.
tools/.model/
.venv-neural/

.DS_Store

# Python bytecode
__pycache__/
*.pyc
